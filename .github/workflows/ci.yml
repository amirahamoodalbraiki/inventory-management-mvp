<<<<<<< HEAD
name: CI Build

on:
  push:
    branches: [ develop, main ]
  pull_request:
    branches: [ develop, main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Backend build (Spring Boot placeholder)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Build backend
        run: |
          cd backend || exit 0
          mvn -q -DskipTests package || echo "Backend placeholder build"

      # Frontend build (React placeholder)
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Build frontend
        run: |
          cd frontend || exit 0
          npm install || echo "Frontend placeholder install"
          npm run build || echo "Frontend placeholder build"
=======
name: CI Build

on:
  push:
    branches: [ develop, main ]
  pull_request:
    branches: [ develop, main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Backend build (Spring Boot placeholder)
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Build backend
        run: |
          cd backend || exit 0
          mvn -q -DskipTests package || echo "Backend placeholder build"

      # Frontend build (React placeholder)
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Build frontend
        run: |
          cd frontend || exit 0
          npm install || echo "Frontend placeholder install"
          npm run build || echo "Frontend placeholder build"
>>>>>>> 2cd5e680925545057079c968ce11fa15fea323a0
